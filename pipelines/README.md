## Pipelines

This folder groups the experiment code by method.

- `ablation/`: orthogonal ablation scripts and related SLURM jobs
- `dpo/`: DPO training and evaluation pipeline
- `deploy/`: helper scripts for pushing jobs and adapters to iLabs

The files here are symlinked so the original repo paths remain valid.
