## Benchmarks

This folder is the single place to find the evaluation assets used to produce the project results.

- `datasets/`: benchmark datasets and DPO preference data
- `generation/`: generative PII leakage benchmark scripts
- `mmlu/`: MMLU evaluation scripts

Most entries here are symlinks to the original files so existing commands still work.
